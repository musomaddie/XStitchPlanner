def create_style(values: dict) -> str:
    """
    Given the provided dictionary of values (like a json object) creates and returns a string that can be used to
    style the qt object which called this. """
    pass
